# Core dependencies
fastapi==0.104.1
uvicorn==0.24.0
pydantic==2.5.0
websockets>=11.0.3

# ML and data processing
numpy>=1.26.0,<2.0  # NumPy 2.x is incompatible with torch 2.1.0 and spacy 3.7.2
scikit-learn>=1.3.2
pandas>=2.1.3

# Audio processing
librosa>=0.10.0
soundfile>=0.12.1
scipy>=1.11.4

# Speech Recognition & Whisper
openai-whisper

# Noise reduction
noisereduce==3.0.3

# File upload handling
python-multipart==0.0.6

# Database (SQLite for dev, extendable to PostgreSQL)
sqlalchemy==2.0.23

# MongoDB
motor>=3.3.0  # Async MongoDB driver
pymongo>=4.5.0  # MongoDB driver

# Testing
requests==2.31.0
pytest==7.4.3

# NLP and Text Processing
spacy==3.7.2
nltk==3.8.1
transformers==4.45.0  # Required for LLaMA 3.2 rope_scaling support
torch==2.1.0
peft>=0.7.0,<0.14.0  # For LoRA adapter loading, compatible with transformers 4.45.0
sentence-transformers>=2.3.0  # Updated for compatibility with huggingface-hub
textblob==0.17.1
langdetect==1.0.9
gensim==4.3.2
accelerate>=0.25.0  # For model loading optimization
huggingface-hub>=0.23.2,<1.0  # Required for transformers 4.45.0

# Development
python-dotenv==1.0.0
